export type LoginFormValues = {
  email: string;
  password: string;
};

export type AddUserFormValues = {
  email: string;
  firstName: string;
  lastName: string;
  school: string;
  userType: string;
};
