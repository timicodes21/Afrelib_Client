export interface INotification {
  src?: string;
  title: string;
  time: string;
  date: string;
}

export interface IResources {
  title: string;
  type: string;
  resource: string;
  sharedBy: string;
}
